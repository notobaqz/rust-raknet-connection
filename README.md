# rust-raknet-connection
a simple project in rust, to connect to a raknet minecraft bedrock server.

```cargo build```
```cargo run main.rs```

project based from https://github.com/TrippleAWap/bedrock-rs/
